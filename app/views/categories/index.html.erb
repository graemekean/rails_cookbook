<div id="bg">
  <img src="http://www.leilappetit.com/blog/wp-content/uploads/2014/06/pimms.jpg?e80df2" alt="">
</div>

<div class='container'>
  <div class='holder'>
  <h1>List of Categories</h1>
    <p>|&nbsp;<% @categories.sort.each do |category| %>
    
    <%= link_to(category.name, category_path(category)) %> &nbsp;   |&nbsp;

    <%#= button_to('Delete Country', country_path(country), method: :delete, data:{confirm: "Delete country? Are you sure?"}) %>
    <%#= link_to("Edit Country", edit_country_path(country)) %>
    
    <% end %></p>
    <br>
    <br>

   
  </div>
</div>